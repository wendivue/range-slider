import { Config } from '../helpers/interface';

const defaultConfig: Config = {
  single: 20,
  from: 20,
  to: 50,
  step: 1,
  inputSingle: 20,
  inputFrom: 20,
  inputTo: 50,
  min: 0,
  max: 1000,
  type: 'double',
  input: true,
  range: true,
  vertical: false,
};

export { defaultConfig };
