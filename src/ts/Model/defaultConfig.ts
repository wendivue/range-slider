import { Config } from '../helpers/interface';

const defaultConfig: Config = {
  single: 20,
  from: 20,
  to: 50,
  step: 1,
  persentFrom: 0,
  persentTo: 0,
  persentSingle: 0,
  min: 0,
  max: 1000,
  type: 'double',
  input: true,
  range: true,
  vertical: false,
};

export { defaultConfig };
